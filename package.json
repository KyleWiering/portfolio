{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Creating a personal home page.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && npm run build:game && npm run build:checkers",
    "build:game": "esbuild src/game.ts --bundle --outfile=dist/game.js --format=esm",
    "build:checkers": "esbuild src/checkers.ts --bundle --outfile=dist/checkers.js --format=esm",
    "dev": "tsc --watch"
  },
  "keywords": [
    "portfolio",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/three": "^0.160.0",
    "esbuild": "^0.19.11",
    "http-server": "^14.1.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
